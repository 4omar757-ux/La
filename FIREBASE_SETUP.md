# ربط التطبيق بـ Firebase

✅ **الإعداد الأساسي خلاص تم** — مشروع Firebase حقيقي متصل بالتطبيق
(`lib/firebase_options.dart`)، وأوضاع اللعب الجماعي تشتغل.

## ⚠️ خطوة أمان باقية عليك (مهمة)

عند إنشاء قاعدة بيانات Firestore أول مرة تكون بوضع **Test mode**، ووضع
Test mode معناه أي جهاز عنده مفاتيح المشروع (وهي موجودة داخل كل نسخة من
التطبيق تلقائياً) يقدر يقرأ ويكتب على أي غرفة لعب — بدون أي تحقق. هذا وضع
مؤقت لأول ٣٠ يوم بس، وبعدها يوقف تماماً إذا ما بدّلته.

**لازم تسوي هذي الخطوة يدوياً من متصفح الجوال (ما أقدر أسويها أنا):**

1. افتح [console.firebase.google.com](https://console.firebase.google.com)
   وادخل على مشروعك.
2. من القائمة يسار الصفحة: **Firestore Database** ← تبويب **Rules** فوق.
3. احذف كل النص الموجود بالمربع، وانسخ محتوى ملف `firestore.rules`
   (موجود بجذر المشروع) كامل والصقه مكانه.
4. اضغط **Publish** فوق يمين الصفحة.

هذا يقفل الثغرة ويخلي كل جهاز يقدر يكتب بس على بياناته هو (اسمه، إجابته،
غرفته اللي أنشأها) مو بيانات أي لاعب أو غرفة ثانية.

## إذا احتجت تربط مشروع Firebase جديد من الصفر

1. افتح [console.firebase.google.com](https://console.firebase.google.com)
   وسجّل دخول بحساب جوجل.
2. اضغط **إنشاء مشروع (Add project)**، واختر أي اسم.
3. اضغط أيقونة **أندرويد** لإضافة تطبيق أندرويد.
4. في خانة **Android package name** اكتب بالضبط:
   ```
   com.hijola.masabaqa
   ```
5. كمّل التسجيل (اسم اللقب اختياري، شهادة SHA-1 اختيارية).
6. بعد التسجيل بيطلع لك ملف `google-services.json` — انسخ محتواه وأرسله
   لي.
7. من **Project settings** (⚙️) فعّل خدمتين:
   - **Firestore Database**: أنشئ قاعدة بيانات، وابدأ بوضع Test mode
     مبدئياً — ثم لا تنسَ خطوة نشر `firestore.rules` أعلاه فوراً.
   - **Authentication**: فعّل طريقة الدخول **Anonymous**.

أرسل لي محتوى `google-services.json` أو القيم (`apiKey`, `appId`,
`messagingSenderId`, `projectId`, `storageBucket`) وأنا أحدّث
`lib/firebase_options.dart` وأبني APK جديد.
