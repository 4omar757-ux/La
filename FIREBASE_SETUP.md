# ربط التطبيق بـ Firebase (لتفعيل أوضاع اللعب الجماعي)

هذي الخطوة الوحيدة اللي لازم تسويها أنت بنفسك (لأنها تحتاج تسجيل دخول بحساب
جوجل)، وتقدر تسويها بالكامل من متصفح الجوال. بعدها أرسل لي القيم وأنا أكمل
الباقي وأبني لك APK جديد.

## الخطوات (من الجوال)

1. افتح [console.firebase.google.com](https://console.firebase.google.com)
   وسجّل دخول بحساب جوجل.
2. اضغط **إنشاء مشروع (Add project)**، واختر أي اسم (مثلاً "مسابقة").
   تقدر توقف Google Analytics إذا ما تحتاجه.
3. بعد إنشاء المشروع، اضغط أيقونة **أندرويد** لإضافة تطبيق أندرويد.
4. في خانة **Android package name** اكتب بالضبط:
   ```
   com.hijola.masabaqa
   ```
5. كمّل التسجيل (اسم اللقب اختياري، شهادة SHA-1 اختيارية ومو ضرورية الآن).
6. بعد التسجيل بيطلع لك ملف اسمه `google-services.json` — نزّله أو افتحه
   وانسخ محتواه.
7. من قائمة المشروع اذهب لـ **Project settings** (⚙️) وفعّل خدمتين:
   - **Firestore Database**: أنشئ قاعدة بيانات (اختر أقرب موقع سيرفر لك،
     وابدأ في وضع Test mode مبدئياً).
   - **Authentication**: فعّل طريقة الدخول **Anonymous**.

## وش ترسل لي بعدين

أرسل لي محتوى ملف `google-services.json` (انسخ النص كامل)، أو صورة شاشة
واضحة لصفحة إعدادات تطبيق الأندرويد في Firebase Console اللي فيها القيم
التالية:

- `apiKey`
- `appId`
- `messagingSenderId` (Project number)
- `projectId`
- `storageBucket`

أنا بعدها أحدّث ملف `lib/firebase_options.dart` بهالقيم، وأشغّل بناء APK
جديد تلقائياً — وأوضاع اللعب الجماعي بتشتغل مباشرة.
